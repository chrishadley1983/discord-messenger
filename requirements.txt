# Core
discord.py>=2.3.0
anthropic>=0.40.0
httpx>=0.27.0
python-dotenv>=1.0.0
apscheduler>=3.10.0

# Supabase (minimal - avoid heavy dependencies)
postgrest>=0.16.0
gotrue>=2.0.0

# Health integrations
garth>=0.4.0
matplotlib>=3.8.0

# RSS/News
feedparser>=6.0.0
beautifulsoup4>=4.12.0

# WhatsApp (optional - install later when needed)
# twilio>=9.0.0

# Browser automation (for Anthropic scraping)
playwright>=1.40.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
freezegun>=1.2.0
