# IHD app server-side secrets.
# Copy this to `.env.local` on the Pi (gitignored) with the REAL values.
# Do NOT commit real values — only this template is committed.
#
# Supabase project: modjoikyuhqzouxvieua  (URL is public, kept in source)

# Anon (public) key — used by /api/energy and /api/kids
SUPABASE_ANON_KEY=

# Service-role key — used ONLY by /api/hb (full DB access; keep secret)
SUPABASE_SERVICE_KEY=

# Hadley Bricks internal API key — used by /api/hb to call the Vercel app
HB_INTERNAL_KEY=
