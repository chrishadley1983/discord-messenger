# Code review — `feature/reset-cut-standing-week` (7 Sep 2026)

Mode: branch (vs `feat/peter-sonnet-5-migration`). Reviewer: independent subagent, every finding
verified against the code by probe. Tests at review time: 234 passed. Verdict: **merge after fixes**.

## Findings and outcome

| Sev | # | Finding | Outcome |
|---|---|---|---|
| Major | M1 | `next_session_type` ignored the standing schedule: on Thursday after a skipped Tuesday it said Lower A while the week view said "Lower A — missed / Thu Upper B" | **Fixed** — schedule-aware `next_session_type(plan, recent, today)`; both service callers pass UK-time today; 8 assertions in `TestReviewFixes` |
| Major | M2 | Fitbod import inferred `upper` / `lower` and, via `reconcile_plan`, grafted a fifth session (`lower`) onto the rotation or wrote into the retired `upper` | **Fixed** — `resolve_session_type` maps regions onto active types (day's scheduled lift if same region, else first of that region); import loop uses it; skill doc updated |
| Major | M3 | Training-tab links were relative (`upper-a.html`) and 404'd on the LAN dashboard at `/fitness/dashboard` | **Fixed** — `GET /fitness/{name}.html` alias delegating to the session-page route |
| Minor | m1 | Non-ISO `schedule_from` raised out of `next_cardio_hard` → 500 on `POST /fitness/cardio` after the row was saved | **Fixed** — `plan_week` returns None on ValueError |
| Minor | m2 | `schedule` accepted None / unknown / retired entries silently | **Fixed** — `schedule_for` validates every entry, falls back to the rotation |
| Minor | m3 | `next_cardio_hard` followed any last modality (a walk became the hard pyramid) | **Fixed** — must be in `modalities` ∪ example |
| Minor | m4 | Patching a retired session type re-added it to the rotation; training-plan skill still steered Peter at `upper` | **Fixed** — no rotation append when `status == retired`; skill triggers/table updated |
| Minor | m5 | `date.today()` (process clock) vs the service's UK `_today()` | **Fixed** — no wall-clock read in the module; callers pass `fit._today()` |
| Minor | m6 | Session pages are public and ungated on surge | **Decision recorded in spec** (acceptable data class); Chris to confirm |
| Minor | m7 | Dashboard build makes four extra `next_session_bundle` round-trips | **Open** — follow-up |
| Minor | m8 | One-element `duration_range_min` crashed the week view | **Fixed** — guarded |
| Nit | n1 | API `reason` strings into innerHTML unescaped (no live risk today) | **Fixed** — `esc()` in runtime.js; action class only from the known set |
| Nit | n3 | `_DAY_NAMES` unused; template hardcoded day badges | **Fixed** — removed; template derives the badge from `t.schedule` |
| Nit | n4 | `_PAGES_DIR` duplicated `LOCAL_PAGES_DIR` | **Fixed** — imported |
| Nit | n2/n5/n6/n7/n8 | Marker replacement confirmed correct; stale defaults for plans without `weekly`; ticks keyed on calendar day; "never two uppers back to back" is text only; Garmin can only infer hard for stairs | **Open** — noted in spec follow-ups |

## Regression sweep
advisor / service / garmin / peter_dashboard: no remaining 3-session, `cardio_easy=5`, `order_variant`
or `upper` assumptions. Skills updated: log-workout, log-cardio, training-plan, fitbod-import,
weekly-cut-review, weekly-health.

## Pages
All four pages: one `<script>`, RUNTIME + TARGETS markers, `data-page`/`data-session`, ordered
slugs equal to the plan's session exercises (enforced by `test_session_pages`), committed runtime
in sync with `runtime.js` (enforced). Browser behaviour S1–S4 checked with Playwright on the built
pages (20/20) — that harness lives outside the repo (scratchpad); the in-repo checks are string-level.

## CLAUDE.md health (this repo)
`CLAUDE.md` is architecture + documentation-location rules, under 200 lines, no feature-specific
content added by this change. No action.
